---
title: "Self-Critique — 2026-07-10 17h00"
slug: "self-critique-2026-07-10-17h"
category: knowledge-synthesis
type: insights
status: draft
created: 2026-07-10 17:00
tags: [self-critique, self-improvement, daily-review]
---

## ❌ Lỗi & Vấp phải

| Session | Lỗi | Nguyên nhân gốc (5 Whys) | Bài học |
|---------|-----|-------------------------|--------|
| `20260710_??` — Self-upgrade lần này | **Memory cập nhật fail** (`memory(target='memory')` thiếu action → `Unknown action 'None'`) | 5 Whys: (1) Gọi `memory(target='memory')` không có action. (2)习惯性 read-memory bằng cách chỉ pass target vì đa số tool cho phép đọc mode khi bỏ action. (3) memory tool schema yêu cầu action bắt buộc — không có default là "read". **Root:** Default read pattern `memory(target=...)` không hoạt động, phải dùng explicit action='add'/'replace'/'remove'. | Memory tool: luôn cần explicit action parameter — không có implicit read mode. Dùng `memory(action='replace', ...)` để cập nhật entry. |
| `20260710_??` — Self-upgrade lần này | **Redundant tool calls**: gọi `skill_view('self-improvement-loop')` 2 lần trong cùng response batch (lần đầu từ skill auto-load, lần 2 do tôi tự gọi lại) | 5 Whys: (1) Gọi lặp `skill_view` cho cùng skill đã được auto-inject. (2) Không kiểm tra xem skill nào đã có trong context trước khi batch load thêm. **Root:** Auto-loaded skills vẫn phải skip khi gọi manual batch nếu trùng name. | Khi agent auto-load skill, kiểm tra response header/toolkit trước khi batch thêm `skill_view` cho cùng tên — tránh waste ~20% tokens/token context. |
| `current` — memory entry maintenance | **15/34 (44%) entries là book summaries stale** — không dùng lại sau nhiều session, không cập nhật action items từ các previous self-critique notes | 5 Whys: (1) Memory grow lên 34 entries mà không có prune pass. (2) Không chạy archive/maintenance định kỳ như hướng dẫn trong vault protocol (monthly/quarterly). (3) Book critique entries ('Uốn lưỡi 7 lần', 'Who Moved My Cheese?', Weston, Brian Tracy...) đã có lesson extracted rồi nhưng vẫn giữ full context thay vì summary rule. **Root:** Memory maintenance cron thiếu chạy định kỳ — không có trigger "memory usage >80% check + archive low-value entries". | Khi memory >80%, chạy pruning pass: convert book summaries → single-line principles, delete duplicate-stale entries. |

## 🔄 Các Pattern lặp lại

1. **Memory bloat** (xuất hiện ~4 lần): Entries accumulate mà không prune → hiện tại 34 entries / 32,768 chars limit = 7,122 chars (~22%). Still low but unstructured — book entries chiếm 59% total memory chars.
2. **Cron deterministic sessions** (xuất hiện ~6 lần): session_search browse trả về các cron daily-morning-ritual (hoàn toàn deterministic) thay vì informative self-critique sessions. Cần filter out before deep analysis.
3. **Self-critique writing delay** (xuất hiện ~3 lần): Self-critique note được tạo xong nhưng action items từ previous critiques (e.g., "fix wiki-schema-versioning-rules.md triple-dash") chưa luôn được verify trong cùng session.

## ✅ Cái gì HIỆU QUẢ

| Session | Approach | Tại sao hiệu quả? |
|---------|----------|------------------|
| Cross-session audit | Browse 5 recent sessions → read_file vào nội dung session JSON → compile errors | Systematic, không bỏ sót error nào từ các sessions trước |
| Ollama list verification | `ollama list` + tự kiểm tra memory note về model quantization — thấy cả 3 quantizations đã có sẵn | Prevents stale recommendations like "consider test lighter models" |
| Config reasoning_effort check | Grep config.yaml → find hai entries `reasoning_effort: high` và `''` | Phát hiện duplicate/conflicting config values mà previous sessions chưa notice |

## 📋 Hành động sửa ngay

- [x] ✅ Patch Open Design memory entry từ `\` sang `/` (forward-slash convention)
- [ ] Update model quantization memory entry từ suggestion → factual
- [ ] Archive/stripping book summary entries thành single-line principles (giảm ~50% memory chars)
- [x] ✅ Thêm rule mới: "memory tool không có implicit read mode — luôn explicit action"

## 🚀 Best Practices phát hiện hôm nay

1. **Self-upgrade pattern**: Khi user nói "tự nâng cấp bản thân", load self-improvement-loop → session_search browse → read_file session contents (không scroll vì quá tải) → compile error inventory → 5 Whys root cause → write Obsidian note + memory updates + verify.
2. **Ollama list trước khi benchmark**: Luôn `ollama list` đầu tiên, tránh propose model quantization chưa tồn tại.
3. **Config double-check**: Dùng grep tìm ALL occurrences của relevant keys — config.yaml có thể chứa duplicate/enabled entries tạo conflict.

---
*Auto-generated by Hermes Agent — 17h00 Daily Review*
